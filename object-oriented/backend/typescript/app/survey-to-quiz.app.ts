import { SurveyToQuizCore } from '../core/logic/survey-to-quiz.core';

export class SurveyToQuizApp {
  constructor(private readonly surveyToQuizCore: SurveyToQuizCore) {}

  public async execute() {}
}
